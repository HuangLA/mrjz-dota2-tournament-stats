# ==========================================
# MRJZ Docker 环境配置
# ==========================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填写下面的配置项（特别是 STEAM_API_KEY）
# 3. 运行 docker-compose up -d
# ==========================================

# ==========================================
# 端口配置
# ==========================================
# 前端访问端口（默认 5173，访问 http://localhost:5173）
# 如果端口被占用，可以改为 8080, 3000 等
FRONTEND_PORT=5173

# 后端 API 端口（默认 3001）
BACKEND_PORT=3001

# MySQL 端口（默认 3306）
# 如果不需要从主机直接访问数据库，可以在 docker-compose.yml 中注释掉 MySQL 的 ports
MYSQL_PORT=3306

# ==========================================
# 数据库配置
# ==========================================
# MySQL root 密码（请修改为强密码）
MYSQL_ROOT_PASSWORD=change_this_root_password

# 数据库名称
MYSQL_DATABASE=mrjz

# 数据库用户名
MYSQL_USER=mrjz_user

# 数据库用户密码（请修改为强密码）
MYSQL_PASSWORD=change_this_user_password

# ==========================================
# API Keys (必需配置)
# ==========================================
# Steam API Key (必需)
# 获取地址: https://steamcommunity.com/dev/apikey
STEAM_API_KEY=YOUR_STEAM_API_KEY_HERE

# Stratz API Token (可选)
# 获取地址: https://stratz.com/api
STRATZ_API_TOKEN=

# ==========================================
# 应用配置
# ==========================================
# 联赛 ID（默认第二届）
LEAGUE_ID=18365

# 定时同步任务 (Cron 表达式: 每天晚上10点)
SYNC_CRON=0 22 * * *

# Node 环境
NODE_ENV=production
